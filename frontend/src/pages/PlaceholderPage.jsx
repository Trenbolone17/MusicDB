export default function PlaceholderPage({ title }) {
  return (
    <section>
      <h1 className="text-2xl font-semibold">{title}</h1>
      <p className="mt-2 text-muted">This page arrives in a later feature.</p>
    </section>
  );
}
